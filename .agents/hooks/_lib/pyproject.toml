[project]
name = "pulse-hooks-lib"
version = "0.1.0"
description = "Shared helpers for genesis Claude Code hooks."
requires-python = ">=3.14"
dependencies = ["pydantic>=2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[dependency-groups]
dev = [
  "pytest>=9.0.3",
]
