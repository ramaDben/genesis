{
  "mcpServers": {
    "pulse-engine": {
      "command": "bash",
      "args": [
        "-c",
        "docker run -i --rm --user \"$(id -u):$(id -g)\" -v \"$(git rev-parse --show-toplevel):/work\" -v pulse-venv:/work/.venv -v uv_warm_cache:/tmp/uv-cache -w /work -e PULSE_WORKSPACE_ROOT=/work -e PULSE_SQLITE_PATH=/work/.pulse/state.sqlite -e UV_HTTP_TIMEOUT=1800 mcp-pulse:0.13.6"
      ]
    }
  }
}
