"""Componentes comunes reutilizables por candidatos de estrategia (dominio puro, solo stdlib).

Sin re-export hacia `genesis.strategy` (R46): los símbolos de este subpaquete se
importan por ruta explícita (p. ej. `genesis.strategy.common.vwap_engine`).
"""
